# Version bump type: patch, minor, major, hotfix, or prerelease
bump = "minor"
# Short description of this release (required)
description = "ASGI sub-app mounting via Router.mount(), WebSocketDisconnect exception, benchmark suite, and a new documentation site."
# Optional context explaining why these changes were made
context = """
Router.mount() enables composing routers and ASGI sub-apps under path prefixes;
downstream consumers already depend on it and are broken against registry 0.1.0.
WebSocketDisconnect adds explicit disconnect detection to WebSocket receive
methods. The docs site (guides, API reference, comparisons) is generated by
selfdoc and deployed via Cloudflare Pages.
"""
# Pre-release identifier: alpha, beta, rc, or stable
# preid = ""
# Set to true to generate a blog post for this release
# blog = false
include = ["pypi"]
exclude = []
